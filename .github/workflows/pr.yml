name: PR test workflow

on:
  pull_request:
    branches: [ "main" ]

jobs:
  run-pr-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show files
        run: |
          echo "CI is running code from PR"
          ls -la
